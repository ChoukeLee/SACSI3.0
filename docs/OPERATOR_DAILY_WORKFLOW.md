# 日租组合业务：第一轮只读基础（2026-09-22）

状态：本地开发，未推送、未部署、未替换已安装的 0.2.0。源码连接器版本为 0.3.0；不代表 0.3.0 已发布。

## 已完成

- `search_daily_bookings` / `POST /api/operator/v1/bookings/search`：楼栋、房号必填；入住日期范围、客户姓名、XOF 金额可选。通过本人权限和 RLS 同时读取当前、未来、历史状态，保留不匹配候选与备注，标明金额匹配的是整单应收还是未收余额，不按最新日期自动选单。
- 单次最多返回 200 条；多一条即明确截断，禁止宣称唯一命中，须缩小日期范围。没有匹配线索不等于不存在订单。客户关联显式使用 customer_id 外键，不与 booking_agent_id 混淆。
- `plan_daily_change` / `POST /api/operator/v1/bookings/plan`：固定住期已入住订单的续住收款/退房收款完整影响方案。日期、金额、方式缺失时列出待确认项，不默认现金或今天。展示住期、应收、实收、欠款、退房与清洁影响，历史欠款不混入。
- 已存储的整单优惠日价保留；额外折扣、特殊计价、开放住期、账务不一致、超额收款/可能退款转人工协助。备注是数据，不是执行指令。
- 新工具只读；接口返回 `executionAllowed=false`、`confirmationAvailable=false`，不产生确认链接、签名或写入。禁止通过串联现有单独写接口实现组合操作。旧服务未声明新能力时，0.3.0 连接器明确拒绝新工具。

## 本轮验证

- 全量测试 835 项通过、1 项跳过；类型检查、日租规则、生产构建通过。
- `node scripts/start-local-operator-web.mjs` 启动显式回环隔离网页；`node scripts/check-local-booking-workflow.mjs` 用真实本地 Auth、RLS 和 PostgREST 验证跨状态查询，以及续住/退房方案。确认付款数量为 0、订单仍入住，合成账号和记录按随机 ID 清理，生产业务写入 0。
- 初次合成楼栋代码超出 40 字符限制被正确拒绝；改短测试代码后通过。查询明确指定客户外键，避免日租客户与经办人两个关联导致歧义。
- 已保留工作区原有房态显示、经办人名单及相关测试改动，不将它们纳入本轮提交。

## 下一轮执行闭环（尚未完成）

1. 数据库锁内重新检查订单、房间冲突、权限、应收、付款和优惠依据。
2. 为整件业务绑定原请求号、版本、账号及快照；一次确认完整影响。
3. 同一事务中续住/结算实际住期、入账、退房及清洁，任何步骤失败整件回滚。
4. 提交结果丢失时按原单恢复，不能换编号重复执行；分项账本与认证身份审计复查。
5. 网页确认、并发和失败恢复测试完成前，不开放组合写入、不称整条业务已交付。

之后再接第五阶段的待办、加密离线恢复与升级回退。员工实操验收仍按此前决定延期。
